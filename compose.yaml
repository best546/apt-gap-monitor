services:
  collector:
    image: ghcr.io/best546/apt-gap-monitor:latest
    pull_policy: always
    env_file: .env
    volumes:
      - ./config:/app/config:ro
      - ./docs/data:/app/docs/data
    restart: "no"
